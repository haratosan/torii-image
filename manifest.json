{
  "name": "image",
  "description": "Generates or edits images via AI. Provide a detailed prompt describing the image to generate or how to edit the attached image.",
  "version": "0.1.0",
  "parameters": {
    "type": "object",
    "properties": {
      "prompt": {
        "type": "string",
        "description": "A detailed description of the image to generate"
      },
      "model": {
        "type": "string",
        "description": "Optional model override (default: google/gemini-2.5-flash-preview:thinking)"
      }
    },
    "required": ["prompt"]
  },
  "env": ["TORII_OPENROUTER_API_KEY", "TORII_IMAGE_MODEL"]
}
